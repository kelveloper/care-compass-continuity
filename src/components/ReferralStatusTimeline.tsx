import React from "react";
import type { PatientDetailViewProps } from "./PatientDetailView";

const ReferralStatusTimeline = (props: PatientDetailViewProps) => {
  return <div>/* Referral status timeline section */</div>;
};

export default ReferralStatusTimeline;
