import React from "react";
import { Patient } from "@/types";

const RiskFactorBreakdown = ({ patient }: { patient: Patient }) => {
  return <div>/* Risk factor breakdown section */</div>;
};

export default RiskFactorBreakdown;
